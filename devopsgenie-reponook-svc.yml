apiVersion: v1
kind: Service
metadata:
  name: dogreponook
  labels:
    app: dogreponook
spec:
  type: LoadBalancer
  ports:
    - port: 8191
      targetPort: dogrepo-port
      protocol: TCP
      name: http
  selector:
    app: dogreponook
